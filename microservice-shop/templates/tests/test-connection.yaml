apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservice-shop.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "microservice-shop.name" . }}
    helm.sh/chart: {{ include "microservice-shop.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "microservice-shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
